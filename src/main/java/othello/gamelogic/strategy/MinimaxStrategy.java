package othello.gamelogic.strategy;

import othello.gamelogic.BoardSpace;
import othello.gamelogic.OthelloGame;
import othello.gamelogic.Player;

/**
 * AI strategy using the Minimax algorithm.
 * Assumes both players play optimally and recursively evaluates future moves.
 */
public class MinimaxStrategy implements Strategy {
    @Override
    public BoardSpace chooseMove(OthelloGame game, Player me) {
        // TODO: Implement Minimax algorithm to choose the optimal move
        return null;
    }
}
